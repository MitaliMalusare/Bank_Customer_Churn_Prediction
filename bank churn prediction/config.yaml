enable_cache: False

settings:
  docker:
    required_integrations:
      - mlflow

# Configuration of the Model Control Plane
model:
  name: Churn_Predictor
  license: Apache 2.0
  description: Bank Customer Churn Rate Prediction.
  tags: ["Classification", "churn_Prediction", "bank"]
